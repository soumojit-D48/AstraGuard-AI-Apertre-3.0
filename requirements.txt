# ===== PRODUCTION DEPENDENCIES (required for runtime) =====

# Core data processing (numpy, pandas lightweight)
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.5.2

# Web framework & async
fastapi==0.128.0
uvicorn[standard]==0.30.0
aiohttp>=3.11.0,<4.0
pydantic==2.7.0
httpx==0.25.0

# ML/AI (CPU-only torch, no CUDA packages - for CI/GitHub Actions compatibility)
# For GPU training locally: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.6.0,<3.0 --no-binary torch
sentence-transformers==5.1.2

# Observability & monitoring
prometheus-client==0.21.0

# Infrastructure & coordination
redis==5.0.1

# Utilities
python-dateutil==2.8.2
pytz==2024.1
protobuf>=4.25.8,<5.0.0
loguru==0.7.2